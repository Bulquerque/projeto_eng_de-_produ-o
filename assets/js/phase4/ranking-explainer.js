const LABELS={total_cost:'custo total',service_quality:'qualidade de serviço',operational_risk:'risco operacional',tax_impact:'impacto tributário',inventory_efficiency:'eficiência de estoque'};
export function explainRanking({objective,scoredScenario}){if(!scoredScenario)return {ranking_explanation:['Nenhum cenário ranqueado ainda.'],top_positive_drivers:[],top_negative_drivers:[]}; const comps=Object.entries(scoredScenario.score_components||{}).sort((a,b)=>b[1]-a[1]); const top=comps.slice(0,2).map(([k])=>k); const weak=comps.slice(-2).map(([k])=>k); const lines=[`O cenário ${scoredScenario.scenario_name||scoredScenario.scenario_id} ficou em rank ${scoredScenario.rank} com score ${Number(scoredScenario.final_score).toFixed(1)}.`]; if(scoredScenario.rank===1)lines.push(`Ele liderou principalmente por ${top.map(k=>LABELS[k]||k).join(' e ')} dentro do objetivo ${objective?.objective_name||'customizado'}.`); if(weak.length)lines.push(`Pontos de atenção no score: ${weak.map(k=>LABELS[k]||k).join(' e ')}.`); return {scenario_id:scoredScenario.scenario_id,ranking_explanation:lines,top_positive_drivers:top,top_negative_drivers:weak}}
