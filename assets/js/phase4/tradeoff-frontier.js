function isDominated(a,b){return b.total_cost<=a.total_cost && b.quality_score>=a.quality_score && (b.total_cost<a.total_cost || b.quality_score>a.quality_score)}
export function buildTradeoffFrontier({companyId,scenarioRecords=[],scoredScenarios=[]}){const scoreMap=new Map((scoredScenarios||[]).map(s=>[s.scenario_id,s])); const points=(scenarioRecords||[]).map(r=>({scenario_id:r.result.scenario_id,scenario_name:r.result.scenario_name,x_total_cost:r.result.total_with_tax,y_quality_score:r.quality.quality_score,risk_level:r.quality.risk_level,final_score:scoreMap.get(r.result.scenario_id)?.final_score??null})); const frontier_points=points.map(p=>({...p,is_frontier_candidate:!points.some(o=>o.scenario_id!==p.scenario_id && isDominated({total_cost:p.x_total_cost,quality_score:p.y_quality_score},{total_cost:o.x_total_cost,quality_score:o.y_quality_score}))})); return {company_id:companyId,frontier_points,warnings:points.length?[]:['Sem pontos para fronteira.'],errors:[]}}
