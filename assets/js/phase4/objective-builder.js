import {validateObjective} from './objective-validator.js';
function slug(v){return String(v||'objetivo').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'')||'objetivo'}
function num(v){const x=Number(v); return Number.isFinite(x)?x:0}
export function normalizeWeightInput(weights){const out={}; for(const [k,v] of Object.entries(weights||{})){out[k]=num(v)} const sum=Object.values(out).reduce((a,b)=>a+b,0); if(sum>1.5 && Math.abs(sum-100)<1e-6){for(const k of Object.keys(out))out[k]=out[k]/100; return out} if(sum>0 && Math.abs(sum-1)>1e-6){for(const k of Object.keys(out))out[k]=out[k]/sum} return out}
export function calculateWeightsSum(weights){return Object.values(weights||{}).reduce((a,b)=>a+num(b),0)}
export function buildObjective({companyId,objectiveName='Objetivo customizado',weights={}}){const normalized=normalizeWeightInput(weights); const objective={objective_id:`${companyId}_${slug(objectiveName)}`,objective_name:objectiveName||'Objetivo customizado',company_id:companyId,weights:normalized,weights_sum:calculateWeightsSum(normalized),normalization:{method:'min_max_by_candidate_set',higher_is_better:['service_quality','inventory_efficiency'],lower_is_better:['total_cost','operational_risk','tax_impact']},metadata:{phase:4,source:'ObjectiveBuilder'}}; const validation=validateObjective(objective); return {...objective,valid:validation.valid,warnings:validation.warnings,errors:validation.errors}}
export function buildObjectivePreviewText(objective){return Object.entries(objective?.weights||{}).map(([k,v])=>`${k}: ${(Number(v)*100).toFixed(0)}%`).join(' · ')}
