import {escapeHtml,formatNumber,formatPct} from '../shared/common.js';
export function renderSearchLog(searchLog){if(!searchLog)return '<div class="empty-state">Otimizador ainda não rodou.</div>'; const method=searchLog.method_applied||searchLog.method_requested||searchLog.method||'—'; const coverage=searchLog.coverage_ratio==null?'—':formatPct(Number(searchLog.coverage_ratio)*100,1); return `<div class="search-log-grid"><div><strong>Estratégia</strong><span>${escapeHtml(searchLog.search_strategy||'—')}</span></div><div><strong>Método</strong><span>${escapeHtml(method)}</span></div><div><strong>Espaço exato</strong><span>${searchLog.exact_search_space?'sim':'não'}</span></div><div><strong>Cobertura</strong><span>${coverage}</span></div><div><strong>Gerados</strong><span>${formatNumber(searchLog.generated_candidates)}</span></div><div><strong>Simulados</strong><span>${formatNumber(searchLog.simulated_candidates)}</span></div><div><strong>Válidos</strong><span>${formatNumber(searchLog.valid_candidates)}</span></div><div><strong>Inválidos</strong><span>${formatNumber(searchLog.invalid_candidates)}</span></div><div><strong>Refino</strong><span>${formatNumber(searchLog.refinement_rounds)} x ${formatNumber(searchLog.refinement_seed_count)}</span></div><div><strong>Melhor score</strong><span>${searchLog.best_score==null?'—':Number(searchLog.best_score).toFixed(1)}</span></div><div><strong>Top cenário</strong><span>${escapeHtml(searchLog.best_scenario_id||'—')}</span></div><div><strong>Melhor custo</strong><span>${escapeHtml(searchLog.best_by_total_cost_scenario_id||'—')}</span></div></div>`}
