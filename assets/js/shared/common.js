export function $(id){return document.getElementById(id)}
export function escapeHtml(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
export function safeNumber(v,d=0){const n=Number(v); return Number.isFinite(n)?n:d}
export function formatNumber(v,d=0){const n=Number(v); return Number.isFinite(n)?n.toLocaleString('pt-BR',{maximumFractionDigits:d,minimumFractionDigits:d}):'—'}
export function formatBRL(v,compact=false){const n=Number(v); if(!Number.isFinite(n))return '—'; if(compact&&Math.abs(n)>=1_000_000)return `R$ ${(n/1_000_000).toLocaleString('pt-BR',{maximumFractionDigits:2})} mi`; return n.toLocaleString('pt-BR',{style:'currency',currency:'BRL',maximumFractionDigits:0})}
export function formatPct(v,d=1){const n=Number(v); return Number.isFinite(n)?`${n.toLocaleString('pt-BR',{maximumFractionDigits:d,minimumFractionDigits:d})}%`:'—'}
export function statusClass(status){const s=String(status||'').toLowerCase(); if(s.includes('ok')||s.includes('ready'))return 'status-ok'; if(s.includes('pending')||s.includes('pendente')||s.includes('atenção')||s.includes('benchmark'))return 'status-warn'; if(s.includes('erro')||s.includes('fail')||s.includes('alto'))return 'status-error'; return 'status-neutral'}
export function metric(label,value,note=''){return `<article class="metric-card"><span class="metric-label">${escapeHtml(label)}</span><strong class="metric-value">${escapeHtml(value)}</strong>${note?`<p class="metric-note">${escapeHtml(note)}</p>`:''}</article>`}
export function renderTable(rows, cols, max=12){const body=rows.slice(0,max).map(r=>`<tr>${cols.map(c=>{const raw=typeof c.value==='function'?c.value(r):r[c.value]; const val=c.format?c.format(raw,r):raw; return `<td>${escapeHtml(val)}</td>`}).join('')}</tr>`).join(''); return `<table><thead><tr>${cols.map(c=>`<th>${escapeHtml(c.label)}</th>`).join('')}</tr></thead><tbody>${body}</tbody></table>${rows.length>max?`<p class="small-note">Mostrando ${max} de ${rows.length} linhas.</p>`:''}`}
