export function serializeScenario(scenario){return JSON.stringify(scenario,null,2)}
export function exportScenarioJson(scenario){const content=serializeScenario(scenario); const filename=`${scenario.scenario_id||'scenario'}.json`; return {filename,content_type:'application/json',content,export_ready:true}}
export function downloadScenarioJson(scenario){const ex=exportScenarioJson(scenario); const blob=new Blob([ex.content],{type:ex.content_type}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=ex.filename; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(url); return ex}
export async function parseImportedScenario(file){const text=await file.text(); let parsed; try{parsed=JSON.parse(text)}catch(e){throw new Error('JSON inválido: '+e.message)} return parsed}
export function validateImportedScenario(companyId,scenario){if(!scenario||typeof scenario!=='object')return {valid:false,error:'Arquivo não contém objeto de cenário.'}; if(scenario.company_id!==companyId)return {valid:false,error:'Cenário pertence a outra empresa.'}; if(!scenario.scenario_id||!scenario.base_scenario_id)return {valid:false,error:'Cenário sem scenario_id ou base_scenario_id.'}; return {valid:true}}
