export function validateCostClosure(costs){const c=costs?.costs||{}; const sum=(+c.transfer_cost||0)+(+c.distribution_cost||0)+(+c.storage_cost||0)+(+c.inventory_cost||0); const total=+c.total_logistics_cost||0; const withTax=+c.total_with_tax||0; const tax=+c.tax_impact||0; return {logisticsClosed:Math.abs(sum-total)<0.05, withTaxClosed:Math.abs(total+tax-withTax)<0.05, sum, total, withTax}}
