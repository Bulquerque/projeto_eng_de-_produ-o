export function summarizeFlows(flows=[]){const destinations=new Set();const cds=new Set();let totalRevenue=0,totalWeight=0;for(const f of flows){if(f.destination_uf||f.destination)destinations.add(f.destination_uf||f.destination);if(f.cd_uf||f.cd)cds.add(f.cd_uf||f.cd);totalRevenue+=Number(f.revenue??f.annual_revenue??0)||0;totalWeight+=Number(f.annual_weight_kg??f.volume??0)||0}return {total_flows:flows.length,destinations_covered:destinations.size,cds_used:cds.size,total_revenue:totalRevenue,total_weight_or_volume:totalWeight}}
export function validateFlowCoverage(flows=[]){const errors=[],warnings=[];for(const [idx,f] of flows.entries()){if(!(f.cd_uf||f.cd))errors.push(`Fluxo ${idx} sem CD.`);if(!(f.destination_uf||f.destination))warnings.push(`Fluxo ${idx} sem destino explícito.`);const vol=Number(f.annual_weight_kg??f.volume??0);if(vol<0)errors.push(`Fluxo ${idx} com volume negativo.`)}return {valid:errors.length===0,errors,warnings}}
