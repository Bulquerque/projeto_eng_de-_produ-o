export function summarizeTaxResults(taxBlock={}){const tax=taxBlock.tax_results||{},coverage=taxBlock.tax_coverage||{},recon=tax.tax_reconciliation||taxBlock.tax_reconciliation||{};return {icms_estimated:Number(tax.icms_estimated||0),difal_estimated:Number(tax.difal_estimated||0),total_tax_impact:Number(tax.total_tax_impact||0),coverage_pct:Number(coverage.coverage_pct||0),flows_with_tax_data:Number(coverage.flows_with_tax_data||0),flows_without_tax_data:Number(coverage.flows_without_tax_data||0),tax_reconciliation:recon,warnings:[...(taxBlock.warnings||[])]}}
export function validateTaxResults(taxBlock={}){const summary=summarizeTaxResults(taxBlock);const errors=[];const warnings=[...summary.warnings];for(const key of ['icms_estimated','difal_estimated','total_tax_impact'])if(summary[key]<0)errors.push(`${key} negativo.`);if(summary.coverage_pct<0||summary.coverage_pct>100)warnings.push('Cobertura tributária fora de 0–100%.');return {valid:errors.length===0,summary,errors,warnings}}
