export function extractReferenceResults(bundle={}){const fit=bundle.base_fit||{};if(fit.status==='benchmark_pending')return {company_id:bundle.model?.company_id,reference_results:null,reference_status:'benchmark_pending',source:null,warnings:['Benchmark consolidado ausente.']};const rows=fit.errors_by_metric||[];const reference_results={};for(const row of rows)reference_results[row.metric]=row.reference;return {company_id:bundle.model?.company_id,reference_results,reference_status:rows.length?'available':'partial_reference',source:fit.reference_source||'base_fit',warnings:[...(fit.warnings||[])]}}
export function validateReferenceCompleteness(reference){if(reference.reference_status==='benchmark_pending')return {valid:true,warnings:['Sem referência consolidada.'],errors:[]};const errors=[],warnings=[];if(!reference.source)warnings.push('Fonte de referência ausente.');if(!reference.reference_results||!Object.keys(reference.reference_results).length)warnings.push('Referência parcial ou vazia.');return {valid:errors.length===0,errors,warnings}}
