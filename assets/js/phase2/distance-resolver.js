export function normalizeLocationName(value){return String(value??'').trim().toUpperCase()}
export function validateDistanceValue(value){const n=Number(value);return {valid:Number.isFinite(n)&&n>=0,value:Number.isFinite(n)?n:null}}
export function resolveDistance(distanceRows=[],origin,destination,{allowReverse=true}={}){const o=normalizeLocationName(origin),d=normalizeLocationName(destination);const exact=distanceRows.find(r=>normalizeLocationName(r.origin||r.origem||r.Origem)===o&&normalizeLocationName(r.destination||r.destino||r.Destino)===d);if(exact){const val=validateDistanceValue(exact.distance_km||exact.distancia_km||exact.distancia||exact.Distancia);return {...val,origin:o,destination:d,distance_status:'exact_match'}}if(allowReverse){const rev=distanceRows.find(r=>normalizeLocationName(r.origin||r.origem||r.Origem)===d&&normalizeLocationName(r.destination||r.destino||r.Destino)===o);if(rev){const val=validateDistanceValue(rev.distance_km||rev.distancia_km||rev.distancia||rev.Distancia);return {...val,origin:o,destination:d,distance_status:'reverse_match'}}}return {valid:false,value:null,origin:o,destination:d,distance_status:'missing',warnings:[`Distância não encontrada para ${o} → ${d}.`]}}
