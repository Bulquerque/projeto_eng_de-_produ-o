# Contratos dos módulos

Esta pasta contém a documentação estruturada dos módulos de todas as fases.

Principais arquivos:

```text
module-contracts.json
module_dependency_matrix.csv|json
module_tests_matrix.csv|json
```

Cada módulo possui fase, descrição, input JSON, output JSON, funções internas, chamadas externas e testes.
